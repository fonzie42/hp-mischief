import { FC } from "react"

{{#if includeSkeletonLoader }}
import Skeleton from "react-loading-skeleton"
{{/if}}
import { Wrapper } from "./{{> ComponentFileName}}.styled"
import { {{> ComponentViewName}}Props } from "./types"

{{#if includeSkeletonLoader }}
export const {{> ComponentViewName}}: FC<{{> ComponentViewName}}Props> = ({ isLoading, label }) => {
{{else}}
export const {{> ComponentViewName}}: FC<{{> ComponentViewName}}Props> = ({ label }) => {
{{/if}}
  {{#if includeSkeletonLoader }}
  if (isLoading) {
    return <Wrapper><Skeleton /></Wrapper>
  }

  return <Wrapper>{label}</Wrapper>
  {{else}}
  return <Wrapper>{label}</Wrapper>
  {{/if}}
}
